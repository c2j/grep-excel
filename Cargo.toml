[package]
name = "search_excel"
version = "0.1.0"
edition = "2021"
description = "TUI tool for searching Excel files with full-text and wildcard search"
license = "MIT"
readme = "README.md"
repository = "https://github.com/c2j/Search_Excel"
keywords = ["excel", "search", "tui", "duckdb"]
categories = ["command-line-utilities", "text-processing"]

[dependencies]
ratatui = "0.30"
crossterm = "0.28"
tui-input = "0.11"
calamine = { version = "0.26", features = ["chrono"] }
duckdb = { version = "1.1", features = ["bundled"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
thiserror = "2"
unicode-width = "0.2"
chrono = "0.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
